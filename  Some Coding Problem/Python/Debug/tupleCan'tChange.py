# question:
# a = (1, 2, 3, 4, 5)
# a[0] = 10
# print(a)

# answer:
# TypeError: 'tuple' object does not support item assignment. It means that we cannot change the value of a tuple once it is created. Tuples are immutable in Python, which means that their contents cannot be modified after they are defined.

a = (1,2,3)
print(a)